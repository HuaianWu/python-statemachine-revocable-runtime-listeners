为承载多租户插件和请求级观测器的长生命周期状态图增加可撤销的运行时监听器订阅：在保留现有 add_listener 链式调用兼容性的同时，提供显式注销以及同步和异步上下文作用域，按对象身份重复注册不得造成重复回调，注销应幂等且只影响运行时加入的目标监听器；事件处理过程中发生增删时，当前宏步使用开始时的一致监听器集合，变更从下一宏步生效，类级监听器和 model 回调始终保留，并且实例初始化后临时加入异步监听器也能被正确调度和移除，不因并发发送、回调异常、序列化或恢复而泄漏、复活或误删其他监听器。

schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/python-statemachine-revocable-runtime-listeners"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh00e0f297763ac6c1fe62040a3e2f1c"
task_id = "python-statemachine-revocable-runtime-listeners"
display_title = "Add revocable, concurrency-safe runtime listener subscriptions"
display_description = "Let long-lived state machines register and remove runtime listeners with identity-based deduplication, idempotent removal, and per-macrostep consistency."
original_title = "支持可撤销且并发安全的运行时监听器订阅"
category = "feature_request"
language = "python"
repository_url = "https://github.com/fgmacedo/python-statemachine"
base_commit_hash = "78e2b4c919faa2ac1c5196cb6ad5245fe4de7ee5"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary 78e2b4c919faa2ac1c5196cb6ad5245fe4de7ee5 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh00e0f297763ac6c1fe62040a3e2f1c-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
